# Compose variable substitution (build args + API CORS).
# Copy from .env.ui.example / .env.node.example as needed.
VITE_API_URL=http://plan-api.ioak.io
VITE_POSTGREST_URL=http://localhost:4001
CORS_ORIGIN=http://plan.ioak.io,https://plan.ioak.io
