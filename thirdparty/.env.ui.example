# Browser-reachable URLs baked into the UI at image build time.
# Use public hostnames (not localhost) when serving behind reverse proxy.
VITE_API_URL=http://plan-api.ioak.io
VITE_POSTGREST_URL=http://plan-pg.ioak.io
# VITE_POSTGREST_JWT=
